import React from 'react'

export default function Tag(props) {

    /* How to make tags render in random colors?*/

    return (
        <div className="profile-tag">
            {props.name}
        </div>
    )
}
